import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { InjectModel } from '@nestjs/mongoose';
import { Model } from 'mongoose';
import { Create{{className}}Command } from '../impl/create-{{camelCaseName}}.command';
import { {{className}}, {{className}}Document } from '../../../schemas/{{camelCaseName}}.schema';

@CommandHandler(Create{{className}}Command)
export class Create{{className}}Handler implements ICommandHandler<Create{{className}}Command> {
  constructor(
    @InjectModel({{className}}.name) private {{camelCaseName}}Model: Model<{{className}}Document>,
  ) {}

  async execute(command: Create{{className}}Command): Promise<{{className}}> {
    const { dto } = command;
    const createdEntity = new this.{{camelCaseName}}Model(dto);
    return createdEntity.save();
  }
}